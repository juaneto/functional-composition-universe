package scalacon.webapp

object Config {

  /**
   * Show in the screen the slider to change the mass of the Sun dynamically
   */
  val sliderActive: Boolean = false

  /**
   * If true when an element is in the same position than another it produces a collision (change image for explosion)
   */
  val collisions: Boolean = false

}
